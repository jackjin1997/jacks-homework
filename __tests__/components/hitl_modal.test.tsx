import { describe, it, expect } from "vitest";
import { HITLModal } from "@/components/HITLModal";

describe("HITLModal export", () => {
  it("is a function", () => {
    expect(typeof HITLModal).toBe("function");
  });
});
